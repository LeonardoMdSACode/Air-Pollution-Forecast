{
    // Add this line to disable Copilot Completions globally
    "github.copilot.enable": {
        "*": false
    },
    // You may also need to set this to false to ensure all inline suggestions are off
    "editor.inlineSuggest.enabled": false
}